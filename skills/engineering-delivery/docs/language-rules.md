# Language Rules

Always answer in the language used by the user.

Do not switch languages unless the user explicitly asks for a different language.

Keep engineering terminology natural for the user's language. Preserve code, identifiers, file names, framework names, and product names as written.

If the user mixes languages, prefer the language used for the actual request.
